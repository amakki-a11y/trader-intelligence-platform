# Phase 4: Dashboard — React + WebSocket + Dealer UX

Read the full prompt from the downloaded file `claude_code_phase4.md` — it contains the complete spec.
This is a pointer file. The actual prompt was saved via Claude's file system.
